---
// Scroll Story Hero - No generic "welcome to my portfolio" nonsense
---

<section class="hero-container">
  <!-- Section 1: The Hook -->
  <div class="story-section hook-section">
    <div class="content-wrapper">
      <div class="glitch-text">
        <span class="line line-1" data-text="Curiosity got me here.">Curiosity got me here.</span>
        <span class="line line-2" data-text="Still figuring out the rest.">Still figuring out the rest.</span>
      </div>
      <div class="scroll-hint">
        <span>scroll to discover</span>
        <div class="scroll-line"></div>
      </div>
    </div>
  </div>

  <!-- Section 2: The Identity -->
  <div class="story-section identity-section">
    <div class="content-wrapper">
      <div class="identity-reveal">
        <div class="name-block">
          <span class="label">they call me</span>
          <h1 class="name">Kannan</h1>
        </div>
        <div class="roles-block">
          <div class="role" data-index="1">
            <span class="role-icon">&#x1F6E1;</span>
            <span class="role-text">Security breaker by profession</span>
          </div>
          <div class="role" data-index="2">
            <span class="role-icon">&#x1F528;</span>
            <span class="role-text">Maker of physical things</span>
          </div>
          <div class="role" data-index="3">
            <span class="role-icon">&#x1F916;</span>
            <span class="role-text">Automator of everything else</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Section 3: The Philosophy -->
  <div class="story-section philosophy-section">
    <div class="content-wrapper">
      <div class="philosophy-text">
        <p class="big-quote">
          "If it exists, it can be <span class="highlight">hacked</span>.<br/>
          If it doesn't exist, it can be <span class="highlight">built</span>."
        </p>
        <p class="small-text">
          DevSecOps by day. 3D printer whisperer by night.<br/>
          I turn coffee into secure code and filament into functional tools.
        </p>
      </div>
    </div>
  </div>

  <!-- Section 4: The Business -->
  <div class="story-section business-section">
    <div class="content-wrapper">
      <div class="business-content">
        <div class="business-header">
          <span class="business-label">Beyond Code</span>
          <h2 class="business-title">I Build Infrastructure You <span class="own-text">Own</span></h2>
        </div>

        <div class="business-services">
          <div class="business-service">
            <span class="service-icon">&#x1F3E2;</span>
            <span class="service-name">Private Cloud</span>
          </div>
          <div class="business-service">
            <span class="service-icon">&#x2699;</span>
            <span class="service-name">DevSecOps</span>
          </div>
          <div class="business-service">
            <span class="service-icon">&#x1F6E1;</span>
            <span class="service-name">Security</span>
          </div>
        </div>

        <p class="business-pitch">
          Stop renting your data to Big Tech.<br/>
          Sovereign infrastructure for UAE businesses.
        </p>

        <a href="/services" class="btn-services">
          <span>Explore Services</span>
          <svg viewBox="0 0 24 24" class="arrow-icon">
            <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" fill="none"/>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <!-- Section 5: The Invitation -->
  <div class="story-section invite-section">
    <div class="content-wrapper">
      <div class="invite-content">
        <h2 class="invite-title">Curious?</h2>
        <p class="invite-text">See what happens when security meets creativity</p>
        <div class="action-buttons">
          <a href="/projects" class="btn-explore">
            <span>Explore the chaos</span>
            <svg viewBox="0 0 24 24" class="arrow-icon">
              <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" fill="none"/>
            </svg>
          </a>
          <a href="/about" class="btn-story">or hear the full story</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-container {
    background: var(--bg-dark, #0a0a0f);
  }

  .story-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .content-wrapper {
    max-width: 1200px;
    padding: 2rem;
    width: 100%;
  }

  /* Section 1: The Hook */
  .hook-section {
    background: linear-gradient(135deg, #0a0a0f 0%, #1a1a2e 50%, #0a0a0f 100%);
  }

  .glitch-text {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .glitch-text .line {
    font-size: clamp(2.5rem, 8vw, 6rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    position: relative;
    display: block;
  }

  .line-1 {
    color: #ff6b6b;
    animation: glitch-1 3s infinite;
  }

  .line-2 {
    color: #4ecdc4;
    animation: glitch-2 3s infinite;
    animation-delay: 0.5s;
  }

  @keyframes glitch-1 {
    0%, 90%, 100% { transform: translate(0); opacity: 1; }
    92% { transform: translate(-5px, 2px); opacity: 0.8; }
    94% { transform: translate(5px, -2px); opacity: 0.9; }
  }

  @keyframes glitch-2 {
    0%, 90%, 100% { transform: translate(0); opacity: 1; }
    91% { transform: translate(3px, -1px); opacity: 0.9; }
    93% { transform: translate(-3px, 1px); opacity: 0.8; }
  }

  .scroll-hint {
    position: absolute;
    bottom: 3rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    color: #666;
    font-size: 0.875rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .scroll-line {
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, #4ecdc4, transparent);
    animation: scroll-pulse 2s ease-in-out infinite;
  }

  @keyframes scroll-pulse {
    0%, 100% { opacity: 0.3; height: 40px; }
    50% { opacity: 1; height: 60px; }
  }

  /* Section 2: Identity */
  .identity-section {
    background: #0d0d12;
  }

  .identity-reveal {
    display: grid;
    gap: 4rem;
  }

  .name-block .label {
    font-size: 0.875rem;
    color: #666;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    display: block;
    margin-bottom: 0.5rem;
  }

  .name-block .name {
    font-size: clamp(4rem, 15vw, 12rem);
    font-weight: 900;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    letter-spacing: -0.05em;
  }

  .roles-block {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .role {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 1.25rem;
    color: #888;
    opacity: 0;
    transform: translateX(-20px);
    animation: role-reveal 0.6s ease forwards;
  }

  .role[data-index="1"] { animation-delay: 0.2s; }
  .role[data-index="2"] { animation-delay: 0.4s; }
  .role[data-index="3"] { animation-delay: 0.6s; }

  @keyframes role-reveal {
    to { opacity: 1; transform: translateX(0); }
  }

  .role-icon {
    font-size: 1.5rem;
  }

  .role-text {
    font-family: 'JetBrains Mono', monospace;
  }

  /* Section 3: Philosophy */
  .philosophy-section {
    background: linear-gradient(180deg, #0d0d12 0%, #1a1a2e 100%);
  }

  .philosophy-text {
    text-align: center;
  }

  .big-quote {
    font-size: clamp(1.5rem, 4vw, 3rem);
    font-weight: 300;
    line-height: 1.4;
    color: #ccc;
    margin-bottom: 2rem;
  }

  .highlight {
    color: #4ecdc4;
    font-weight: 600;
    position: relative;
  }

  .highlight::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 2px;
    background: #4ecdc4;
    transform: scaleX(0);
    animation: underline-grow 1s ease forwards 1s;
  }

  @keyframes underline-grow {
    to { transform: scaleX(1); }
  }

  .small-text {
    font-size: 1.125rem;
    color: #666;
    line-height: 1.8;
  }

  /* Section 4: Business */
  .business-section {
    background: linear-gradient(180deg, #1a1a2e 0%, #0d0d12 100%);
  }

  .business-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .business-header {
    margin-bottom: 2rem;
  }

  .business-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #4ecdc4;
    padding: 0.5rem 1rem;
    background: rgba(78, 205, 196, 0.1);
    border-radius: 9999px;
    margin-bottom: 1rem;
  }

  .business-title {
    font-size: clamp(2rem, 6vw, 3.5rem);
    font-weight: 800;
    line-height: 1.2;
  }

  .own-text {
    background: linear-gradient(135deg, #667eea 0%, #4ecdc4 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .business-services {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin: 2rem 0;
    flex-wrap: wrap;
  }

  .business-service {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    min-width: 140px;
    transition: all 0.3s ease;
  }

  .business-service:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(78, 205, 196, 0.3);
    transform: translateY(-3px);
  }

  .business-service .service-icon {
    font-size: 2rem;
  }

  .business-service .service-name {
    font-size: 0.875rem;
    color: #888;
    font-weight: 500;
  }

  .business-pitch {
    font-size: 1.125rem;
    color: #666;
    line-height: 1.6;
    margin-bottom: 2rem;
  }

  .btn-services {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background: transparent;
    color: #4ecdc4;
    font-size: 1rem;
    font-weight: 600;
    border: 2px solid #4ecdc4;
    border-radius: 9999px;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-services:hover {
    background: rgba(78, 205, 196, 0.1);
    transform: translateY(-3px);
    box-shadow: 0 10px 40px rgba(78, 205, 196, 0.2);
  }

  .btn-services .arrow-icon {
    width: 20px;
    height: 20px;
    transition: transform 0.3s ease;
  }

  .btn-services:hover .arrow-icon {
    transform: translateX(5px);
  }

  /* Section 5: Invite */
  .invite-section {
    background: radial-gradient(ellipse at center, #1a1a2e 0%, #0a0a0f 70%);
  }

  .invite-content {
    text-align: center;
  }

  .invite-title {
    font-size: clamp(3rem, 10vw, 8rem);
    font-weight: 900;
    color: #fff;
    margin-bottom: 1rem;
  }

  .invite-text {
    font-size: 1.25rem;
    color: #888;
    margin-bottom: 3rem;
  }

  .action-buttons {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }

  .btn-explore {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    font-size: 1.125rem;
    font-weight: 600;
    border-radius: 9999px;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-explore:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 40px rgba(102, 126, 234, 0.4);
  }

  .btn-explore .arrow-icon {
    width: 24px;
    height: 24px;
    transition: transform 0.3s ease;
  }

  .btn-explore:hover .arrow-icon {
    transform: translateX(5px);
  }

  .btn-story {
    color: #666;
    text-decoration: none;
    font-size: 1rem;
    transition: color 0.3s ease;
  }

  .btn-story:hover {
    color: #4ecdc4;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .content-wrapper {
      padding: 1.5rem;
    }

    .roles-block {
      gap: 1rem;
    }

    .role {
      font-size: 1rem;
    }

    .action-buttons {
      width: 100%;
    }

    .btn-explore {
      width: 100%;
      justify-content: center;
    }
  }

  /* Light mode adjustments */
  :global(html.light) .hero-container {
    background: #fafafa;
  }

  :global(html.light) .hook-section {
    background: linear-gradient(135deg, #ffffff 0%, #f0f0f5 50%, #ffffff 100%);
  }

  :global(html.light) .identity-section {
    background: #f5f5f7;
  }

  :global(html.light) .philosophy-section {
    background: linear-gradient(180deg, #f5f5f7 0%, #e8e8ed 100%);
  }

  :global(html.light) .invite-section {
    background: radial-gradient(ellipse at center, #e8e8ed 0%, #fafafa 70%);
  }

  :global(html.light) .big-quote {
    color: #333;
  }

  :global(html.light) .small-text,
  :global(html.light) .invite-text,
  :global(html.light) .role {
    color: #666;
  }

  :global(html.light) .invite-title {
    color: #111;
  }

  :global(html.light) .scroll-hint {
    color: #999;
  }

  :global(html.light) .business-section {
    background: linear-gradient(180deg, #e8e8ed 0%, #f5f5f7 100%);
  }

  :global(html.light) .business-service {
    background: rgba(0, 0, 0, 0.02);
    border-color: rgba(0, 0, 0, 0.05);
  }

  :global(html.light) .business-service:hover {
    background: rgba(0, 0, 0, 0.04);
  }

  :global(html.light) .business-pitch {
    color: #666;
  }
</style>

<script>
  // Scroll-triggered animations
  const observerOptions = {
    threshold: 0.3,
    rootMargin: '0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in-view');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.story-section').forEach(section => {
    observer.observe(section);
  });
</script>
